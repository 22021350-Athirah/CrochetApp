<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Crochet</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #d3d3d3; 
    }
    .navbar-crochet {
      background-color: #000000; 
    }
    .navbar-crochet .navbar-brand, .navbar-crochet .nav-link {
      color: white;
    }
    .rounded-image {
      width: 150px; 
      height: 150px; 
      object-fit: cover; 
      border-radius: 20px; 
    }
  </style>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
  <nav class="navbar navbar-expand-sm navbar-crochet">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Crochet</a>
    </div>
  </nav>

  <div class="container">
    <br>
    <div class="text-center"><h2>Crochet Details</h2></div>
    <br>
    <% if (crochet) { %>
      <div>
        <p><b>Crochet Category</b><br> <%= crochet.category %></p>
        <p><b>Name:</b><br> <%= crochet.name %></p>
        <p><b>Description:</b><br> <%= crochet.description %></p>
        <p><b>Yarn Type:</b><br> <%= crochet.yarntype %></p>
        <p><b>Project Work:</b><br> 
          <% if (crochet.image.startsWith('http')) { %>
            <img src="<%= crochet.image %>" class="rounded-image" alt="Crochet Image">
          <% } else { %>
            <img src="/images/<%= crochet.image %>" class="rounded-image" alt="Crochet Image">
          <% } %>
        </p>
      </div>
    <% } else { %>
      <p>No crochet found.</p>
    <% } %>
    <a href="/index">Back</a>
  </div>

</body>
</html>

