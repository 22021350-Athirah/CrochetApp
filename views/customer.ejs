<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Customer</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #d3d3d3; 
    }
    .navbar-crochet {
      background-color: #000000; 
    }
    .navbar-crochet .navbar-brand, .navbar-crochet .nav-link {
      color: white;
    }
    .btn-brown {
      background-color: #8b4513; 
      color: white;
    }
    .btn-brown:hover {
      background-color: #a0522d; 
    }
    .customer-image {
      width: 150px; 
      height: 150px; 
      object-fit: cover; 
      border-radius: 20px; 
    }
  </style>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
  <nav class="navbar navbar-expand-sm navbar-crochet">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Customer</a>
    </div>
  </nav>

  <div class="container">
    <br>
    <div class="text-center"><h2>Customer Details</h2></div>
    <br>
    <% if (customer) { %>
      <div>
        <p><b>Customer Name:</b><br> <%= customer.name %></p>
        <p><b>Customer Email:</b><br> <%= customer.email %></p>
        <p><b>Reference Inspo:</b><br> 
          <% if (customer.image.startsWith('http')) { %>
            <img src="<%= customer.image %>" class="customer-image" alt="Customer Image">
          <% } else { %>
            <img src="/images/<%= customer.image %>" class="customer-image" alt="Customer Image">
          <% } %>
        </p>
        <p><b>Contact:</b><br> <%= customer.contact %></p>
      </div>
    <% } else { %>
      <p>No customer found.</p>
    <% } %>
    <a href="/orders">Back</a>
  </div>

</body>
</html>

